server:
  port: 8088
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/vote_db
    username: moussa
    password: P@sser2024
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
